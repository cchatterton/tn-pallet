# TN Pallet

Manage a named colour palette from Appearance > Palette.

Generated CSS is written to:

```text
wp-content/uploads/tn-pallet/palette.css
```

The generated stylesheet includes CSS custom properties and text, border, background, hover text, hover border, and hover background utility classes for every configured colour.
